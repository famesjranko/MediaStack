# Structure

Canonical placement rules for MediaStack. Consumed by contributors and review tooling.
For deep architectural narrative, see `docs/design/architecture.md`.

Generated: 2026-04-22

---

## Tree

```
mediastack                          Interactive launcher (front-door for non-technical users; dispatches to setup.sh and scripts/)
setup.sh                            One-time installation entrypoint (thin orchestrator)
docker-compose.yml                  All 19 services, profiles, single bridge network
config.yml                          User-editable: indexers, quality profiles, paths, libraries
.env.example                        Template for .env (generated by setup.sh)

scripts/
  configure.sh                      Configuration entrypoint — sources lib/ + services/
  update.sh                         Update containers using the selected Stable or Latest image channel
  image-drift.py                    CI/local check for remote image digest drift (no pulls, no DinD)
  port-check.sh                     Port forwarding verification (DNS, TCP 80/443/6881, UDP 51820)
  nvidia-repatch.sh                 Re-apply NVENC patch after driver update
  storage-watchdog.sh               NAS mount/sentinel watchdog for managed network storage
  setup/                            Setup modules sourced by setup.sh
    checks.sh                       Prerequisite checks (root, debian, docker, compose, disk)
    packages.sh                     Base package + Docker installation (--full only)
    gpu.sh                          GPU detection, driver install, nvidia-patch, verification
    override.sh                     Host memory detection, compute limits, generate override
    env_gen.sh                      Interactive .env generation
    storage.sh                      Storage mode helpers, NAS preflight, watchdog install
    wizard.sh                       Interactive setup wizard (quality tier, GPU, subtitles)
    recovery.sh                     Stage re-entry routing and existing-install add-stage menu
    stages/
      stage1.sh                     Stage 1 Core LAN controller and install path
      stage2.sh                     Stage 2 remote-access collection/install controller
      stage3.sh                     Internal hardware transcoding controller, NVIDIA finalize marker helpers, post-reboot finalize helper
    wizard_apply.py                 Apply wizard preset to config.yml (preserves comments)
    presets.yml                     Quality model: resolution × size axes (composed at apply time)
    reboot.sh                       Post-reboot systemd service scheduling + cleanup
    hardening.sh                    OS hardening (UFW, sysctl, auto-updates) + optional SMB
    stack.sh                        Data/config dirs, compose up, health wait, access info
  lib/
    common.sh                       Logging, cfg_* YAML readers, api_get/api_post, key mgmt
    http.sh                         wait_for_service, js_post (cookie-session)
    json.sh                         json_get, json_path, json_has_name, json_array_nonempty
    nvidia_patch.sh                 Shared nvidia-patch pinning and verified execution helpers
    ui.sh + ui_fallback.sh          Interactive prompts (+ non-interactive fallback)
    arr/                            Shared Sonarr/Radarr helpers
      main.sh                       Quality profiles, definitions, custom formats, indexer wiring
      custom_formats.yml            Curated TRaSH Guides format definitions (developer-managed)
      render/*.py                   JSON/XML transforms (Python stdlib only)
  services/<svc>/                   Per-service configurators (13 services; Seerr is services/seerr/)
    main.sh                         configure_<svc>() — required
    templates/*.json                Static API payloads (no variable substitution)

config/                             Live service configs (all gitignored — runtime)
  examples/                         Tracked-in-git config templates
    defaults/                       Pre-seed templates: fail2ban/{action,filter,jail}.d,
                                    homepage/*.yaml, jackett/Jackett/ServerConfig.json,
                                    qbittorrent/qBittorrent/{qBittorrent.conf,categories.json}.
                                    create_config_dirs (seed_config_from_templates in
                                    scripts/setup/stack.sh) copies each to config/<svc>/ on
                                    install if absent; templates survive uninstall/full-wipe.
  fail2ban/, homepage/, jackett/, qbittorrent/   Live copies seeded from examples/defaults/ (gitignored)
  state/                            Runtime state snapshots (gitignored)
    image-policy.tsv                Per-service image overrides — Manage updates menu (ADR-30)

tests/
  run.sh                            Test runner: ./tests/run.sh [scenario] [--keep] [--no-cache]
  Dockerfile.dind + entrypoint      Debian-based DinD image
  lib/                              assert.sh, cache.sh, dind.sh, stack.sh, wizard_pty.py, wizard_stage1_common.sh
  assertions/                       Per-service assertion modules (sourced by scenarios)
  scenarios/                        smoke.sh, fresh-install.sh, ddns-seed.sh, nas-storage.sh, image-override.sh, wizard-ui-*.sh
  unit/                             gpu-branching.sh, hardening.sh, image-drift.sh, port-check.sh, resource-limits.sh, upgrades-manifest.sh, wizard-presets.sh

docs/                               Product and project docs (human-facing)
  README.md                         Docs index and reviewer reading order
  assets/                           Docs images and static media
    mediastack-banner.png           README banner
  design/
    architecture.md                 System architecture and service graph
  setup/
    setup-flow.md                   `setup.sh` phases
    configure-flow.md               `scripts/configure.sh` service wiring
    configuration-schema.md         `config.yml` + `.env.example` reference
    storage.md                      Local, managed NFS/NAS, and manual storage modes
  operations/
    day-2.md                        Updates, recovery, and troubleshooting
    upgrades.md                     Per-service image pin policy + candidate-image preflight
    image-updates.md                Maintainer image-drift workflow
    image-digests.lock              Stable-channel image digest record accepted by maintainer preflight
  reference/
    quality-bounds.md               Quality profile bounds and tuning reference
  testing/
    README.md                       DinD and CI test-surface guide
  project/
    stack.md                        Runtime, services, commands, and host dependencies
    structure.md                    This placement contract
  private/                          Private maintainer notes, excluded from public sync
```

## Dependency Direction

```
scripts/services/*                → lib/common.sh + lib/http.sh + lib/json.sh
scripts/services/{sonarr,radarr}  → lib/arr/  (additionally)
scripts/services/*                ↛ scripts/services/*  (no cross-service imports)
setup.sh                          → lib/common.sh + scripts/setup/*.sh
scripts/setup/*                   → lib/common.sh (via setup.sh sourcing)
scripts/setup/*                   ↛ scripts/setup/* (no cross-module imports)
tests/scenarios/*                 → tests/lib/* + tests/assertions/*
```

Cross-service logic that touches 2+ configurators goes in `scripts/flows/*.sh`
(loaded by configure.sh; empty today).

## Adding a New Service

1. Add container to `docker-compose.yml` (image, ports, healthcheck, volumes, profile). Pick a free host octet from the `mediastack` IP map in the network-block comment and express it as `${MEDIASTACK_NETWORK_PREFIX}.X` — every bridge service is pinned, no auto-allocation. `network_mode: host` services skip this.
2. Create `scripts/services/<svc>/main.sh` with `configure_<svc>()` function
3. Add `<svc>` to the loop in `scripts/configure.sh` (ordering matters — after dependencies)
4. Add config.yml section if the service needs user-editable parameters
5. Extract `templates/*.json` only for static payloads (no variable substitution) with 4+ fields
6. Update this file's tree

## Bumping a Service Version

Image tags live in `docker-compose.yml` (the source of truth); `docs/operations/upgrades.md` records each
service's pin policy and preflight command. To bump a service safely:

1. Pick the candidate tag. Read the service's row in `docs/operations/upgrades.md` (pin policy + touchpoint).
2. Preflight it **without editing compose** — patch the DinD copy and run the service's oracle:
   `MS_TEST_IMAGE_OVERRIDES="<svc>=<candidate-ref>" ./tests/run.sh <preflight-scenario>`
   (most services use `fresh-install`; wireguard → `wireguard`, npm → `npm-heal`, fail2ban →
   `fail2ban-drift`, ddns-updater → `ddns-seed`; always read the service's row for the exact token).
3. If the API/env/config shape changed, update the touchpoint files (service `main.sh`, and where
   relevant `scripts/setup/env_gen.sh`, `stages/stage2.sh`, `scripts/lib/`, pre-seeded `config/`)
   and record the rationale in the maintainer decision log when the change is behavioral.
4. Edit the tag in `docker-compose.yml`. If the pin **level** changed (e.g. `:latest` → exact patch),
   update the service's `docs/operations/upgrades.md` pin-policy token and the matching image-pin policy note.
5. Re-run the preflight scenario + `./tests/run.sh smoke`; run `./tests/unit/upgrades-manifest.sh`.
6. If the scheduled image-drift workflow alerted, refresh `docs/operations/image-digests.lock` only after the local DinD preflight passes. Stable-channel installs pin runtime image refs to this lock through `docker-compose.override.yml`.
7. Major / API-unstable bumps are clean cutover (Invariant 2): `docker compose down -v && ./setup.sh --full`.

Preflight checks API **shape** only (via the service's scenario/assertions oracle); it does not catch
config-time breaks. For a major bump, run the service's own battery plus `fresh-install` where
relevant — `fresh-install` does **not** start the `remote` (wireguard) or `subtitles` (bazarr)
profiles, so those need their own scenarios (wireguard: `smoke` + `wireguard` + the access-tier
scenarios).

## Evolution Triggers

- Service main.sh > 200 lines → extract helpers within the service dir
- Feature touches 2+ services → `scripts/flows/*.sh`, not inline in a service
- New shared Sonarr/Radarr pattern → `scripts/lib/arr/`
- New test scenario → `tests/scenarios/<name>.sh` + register in run.sh case statement
- Pre-seeded config for a new service → add the template under `config/examples/defaults/<svc>/…` (tracked) and gitignore the live `config/<svc>/` copy; `seed_config_from_templates` copies it on install automatically
