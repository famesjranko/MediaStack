# Secrets
.env
.env.gcp
tests/gcp-mediastack-plan.md

# NVIDIA patch repo + driver download scratch (cloned/used by setup.sh)
.nvidia-patch/
.nvidia-tmp/

# Generated by setup.sh (resource limits + GPU config)
docker-compose.override.yml

# Test runtime artifacts (written inside the DinD copy; never committed)
# ─────────────────────────────────────────────────────────────
tests/.dind-state
tests/.image-override-applied
.tmp/

# Service runtime configs (generated by containers)
config/jellyfin/
config/sonarr/
config/radarr/
config/jellyseerr/
config/unpackerr/
# Homepage runtime artifacts (pre-seeded YAML configs are tracked in git)
config/homepage/services.yaml
config/homepage/logs/
config/npm/
config/state/
config/wireguard/
config/ddns-updater/
config/bazarr/
config/uptime-kuma/
config/beszel/

# qBittorrent runtime data (keep pre-seeded defaults)
config/qbittorrent/qBittorrent/data/
config/qbittorrent/qBittorrent/ipc-socket
config/qbittorrent/qBittorrent/lockfile
config/qbittorrent/qBittorrent/config/

# Backups
backups/

# Local agent/editor state
.agents/

# Local plan files
docs/plans

# Python bytecode (wizard helpers regenerate this on import)
__pycache__/
*.pyc
